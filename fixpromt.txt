🎯 Задача:
— Найди и исправь ошибку(и), описанные в логах или сообщении.
— Определи самостоятельно, какие блоки затрагиваются. Перепиши **каждый необходимый блок целиком**.
— Сохраняй стиль проекта: псевдографика «╔…╗», комментарии-шапки, docstring-и, логирование.
— Код должен быть Pylance-clean (без подчёркиваний), с актуальными type hints.
— Обновляй import-ы **только внутри переписанных блоков**.
— В конце каждого блока — секция «История изменений» с датой и кратким описанием.

📝 Формат ответа:
1. Каждый переписанный блок — **в отдельном фрагменте** `python` (только целиком, готов к вставке).
2. Краткое объяснение сути правок (1–2 абзаца).

❗ Не выводи частичных фрагментов и не изменяй код, не связанный с ошибкой.
